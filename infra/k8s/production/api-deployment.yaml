apiVersion: apps/v1
kind: Deployment
metadata:
  name: nuit-one-api
  namespace: nuit-one
  labels:
    app: nuit-one-api
    app.kubernetes.io/name: nuit-one-api
    app.kubernetes.io/part-of: nuit-one
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nuit-one-api
  template:
    metadata:
      labels:
        app: nuit-one-api
        app.kubernetes.io/name: nuit-one-api
        app.kubernetes.io/part-of: nuit-one
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        fsGroup: 1001
        seccompProfile:
          type: RuntimeDefault
      imagePullSecrets:
      - name: ghcr-credentials
      containers:
      - name: nuit-one-api
        image: ghcr.io/madfam-org/nuit-one/api:latest
        ports:
        - containerPort: 3001
          name: http
        envFrom:
        - secretRef:
            name: nuit-one-secrets
            optional: true
        resources:
          requests:
            cpu: "200m"
            memory: "512Mi"
          limits:
            cpu: "1000m"
            memory: "1Gi"
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop: [ALL]
        startupProbe:
          httpGet:
            path: /api/health
            port: 3001
          initialDelaySeconds: 5
          periodSeconds: 5
          failureThreshold: 30
        readinessProbe:
          httpGet:
            path: /api/health
            port: 3001
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /api/health
            port: 3001
          periodSeconds: 30
